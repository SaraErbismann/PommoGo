import { getDatabase, ref, push, update } from 'firebase/database';

export default function dateHelpers() {
    // format dates based on chosen range
    // format comparison dates based on input date

}

export default function getComparisonDate(range) {
    const date = new Date();

}